# План работы

## Подготовка датасета
- Данные уже загружены, есть zip-архив с данными в виде csv-файлов (~260 тысяч записей и размеченные данные - 19 csv файлов каждого класса по 100 записей)
- Предобработка данных для обученя не требуется, коллеги уже удалили подготовили данные. Но в исходном датасете данные не предобработаны, можно на них продемонстрировать необходимые преобразования. Там потребуется:
    - Удаление строк с отсутствующими наименованиями проектов
    - Удаление из наименований проектов обязательной части отвечающей за адрес. Эта часть не несет никакой полезной информации для классификации.
- Объединить размеченные данные в единый датасет и разделение на обучающую и тестовую выборки (или обучающую, тестовую и валидационную выборки).
### Использование аббревиатур
В названиях проеков используется множество аббревиатур (и устойчивых выражений), которые могут быть использованы для классификации проектов. Коллегами на работе был подготовлен словарь аббревиатур, но в нем сразу произведена классификация, т.е. есть знание, что конкретная аббревиатура увеличивает вероятность отнесения проекта к определенному классу, но сама аббревиатура не рассшифровывается. Но пока кажется, что это и не надо.

## Классификация проектов
### Базовая оценка
- Использовать sentence vectorizer ('sberbank-ai/sbert_large_nlu_ru') для векторизации текста.
- Использовать простую линейную регрессию для классификации проектов. От этого будем отталкиваться.

### Использование XGBoost
- Использовать sentence vectorizer ('sberbank-ai/sbert_large_nlu_ru') для векторизации текста.
- Использовать XGBoost для классификации проектов.
- Исследование влияния параметров на результат (тип бустера, learning_rate, max_depth и т.п.).

### Использование CatBoost
- Использовать CatBoostClassifier с встроенными средствами токенизации для классификации проектов.
- Использовать CatBoostClassifier в комбинации с sentence vectorizer ('sberbank-ai/sbert_large_nlu_ru') для классификации проектов.
- Исследование влияния параметров на результат (iterations, learning_rate, depth, l2_leaf_reg и т.п.).
- Исследовать параметр cat_features для учета аббревиатур в классификации проектов.

### Использование аббревиатур для токенизации
- Для всех перечисленных шагов попробовать после токенизации sentence vectorizer-ом произвести конкатенацию с вектором полученным из 
использования аббревиатур в каждом тексте. Т.е. добавить вектор равный длине словаря аббревиатур с нулями и единицамии, т.е. фактом использования той или иной аббревиатуры в тексте, и таким образом постараться учесть влияние аббревиатур.

### Дообучение предобученной BERT модели
- Дообучить ту же sbert_large_nlu_ru модель (или попроще) для классификации проектов. Мой датасет является маленьким, поэтому буду экспериментировать только с финальным слоем класса BertForSequenceClassification.
- Провести эксперименты с скоростью обучения, размером батча и classifier_dropout если замечу переобучение.

## Выводы